[project]
name = "thirteen-training"
version = "0.1.0"
description = "RL training pipeline for Tiến Lên bots"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.0",
    "onnx>=1.14",
    "onnxruntime>=1.16",
]

[tool.uv]
dev-dependencies = []
